# Promoção de extração estruturada residual

Decisão: `PROMOTE_TO_CANARY`. O holdout congelado contém 100 casos; recall crítico 100.00%, schema 100.00%, taxa útil 100.00% e 0 casos críticos.

Canário: `OFFLINE_GATE_PASSED_CANARY_NOT_RUN`. Benchmark e shadow não contam como economia operacional.

Limitações: casos residuais sintéticos controlados; canário de produção e GPT direto não foram executados.
