#!/usr/bin/env node

// Compatibilidade: a separação histórica já foi concluída. A fonte canônica
// atual é o instalador visual, usado também por este comando legado.
await import("./install-alarm-house-flow.mjs");
